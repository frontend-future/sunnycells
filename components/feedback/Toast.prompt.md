Short black confirmation after an action.

```jsx
<Toast action={{ label: 'View bag', onClick: open }}>Added to bag</Toast>
```

- Success and neutral confirmations only. Errors go inline on the field, never in a toast.
- Sentence case, under five words.
- The action link is `--sun`, the one place yellow is used as a text colour, and only on black.
