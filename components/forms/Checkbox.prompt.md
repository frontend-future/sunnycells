Opt-ins and multi-select filters.

```jsx
<Checkbox label="Email me when it ships" checked={on} onChange={setOn} />
```

- The whole row is the hit target and clears 48px.
- Checked state is solid black, never a coloured fill.
