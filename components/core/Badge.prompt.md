Merchandising flag pinned to a product block: `BESTSELLER`, `NEW`, `BACK IN STOCK`.

```jsx
<Badge tone="ink">Bestseller</Badge>
```

- Copy is always ALL CAPS and two words maximum.
- Rectangular (6px), not a pill. Pills are for `Tag`.
- Use `tone="ink"` on a coloured block; use the family tone on white.
