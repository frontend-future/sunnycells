Preference toggle for account and subscription settings.

```jsx
<Switch label="Pause deliveries" checked={paused} onChange={setPaused} />
```

- Use `RadioOption` for anything that changes what the customer pays; a switch is for preferences only.
