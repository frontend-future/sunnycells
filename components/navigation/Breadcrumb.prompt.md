Trail above product and category headings.

```jsx
<Breadcrumb items={[{ label: 'Home', href: '/' }, { label: 'Skin' }]} />
```

- One of the few places 17px is allowed. It is meta, never load-bearing.
