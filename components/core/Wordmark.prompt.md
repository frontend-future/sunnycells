The brand name set in type, used wherever a logo would go.

```jsx
<Wordmark size={28} />
```

- No logo files were provided, so this is intentionally typographic. Do not draw or approximate a mark.
- `tone="inverse"` on black or on a full-bleed accent band that is dark; on yellow, keep it `ink`.
