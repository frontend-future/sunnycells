Section switcher on the PDP.

```jsx
<Tabs items={['Benefits', 'Ingredients', 'Results']} value={tab} onChange={setTab} />
```

- Active tab: 800 weight + 3px black underline. No pill, no fill.
- Four tabs maximum; beyond that use `Accordion`.
