Quantity control in the bag and on the PDP.

```jsx
<QuantityStepper value={qty} onChange={setQty} />
```

- Buttons disable at `min`/`max` rather than disappearing.
- The count uses tabular numerals so the control doesn't jitter.
