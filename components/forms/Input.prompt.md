Labelled text field. Body-size (20px) input text, never shrink it.

```jsx
<Input label="Email" type="email" hint="Order updates only." />
```

- Error copy states what is needed ("We need an email address to send your order."), never "Invalid input".
- Labels are always visible; placeholder-only fields are not allowed at this text size.
